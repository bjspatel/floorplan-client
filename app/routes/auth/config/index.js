/**
 * @file
 * @description Integrates route configs for auth routes
 */
'use strict'

const validationSchema = require('./validation-schema')
const responseMap = require('./response-map')

module.exports = {
  validationSchema,
  responseMap
}
